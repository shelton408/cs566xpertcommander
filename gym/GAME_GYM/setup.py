from setuptools import setup
from setuptools import find_packages


setup(
    name='the_game',
    version='0.0.1',
    description='The Game environment for OpenAi Gym',
    install_requires=['gym', 'numpy'])
